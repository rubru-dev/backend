"use client";
import { Loading, PageTitle, useGet } from "@/components/erp/shared";
export default function OutstandingPage(){const {data,loading}=useGet<any>("/erp/renewals/outstanding");return <div className="p-9"><PageTitle title="Outstanding (OS)" subtitle="Tagihan customer yang belum dibayar."/><div className="card mt-7 overflow-hidden">{loading?<Loading/>:<table><thead><tr><th>No. Tagihan</th><th>Customer</th><th>Keterangan</th><th>Jatuh Tempo</th><th>Nilai</th></tr></thead><tbody>{data?.data?.map((x:any)=><tr key={x.id}><td className="font-semibold text-accent">{x.number}</td><td>{x.customer.name}</td><td>{x.description||"-"}</td><td>{new Date(x.dueDate).toLocaleDateString("id-ID")}</td><td>Rp {Number(x.amount).toLocaleString("id-ID")}</td></tr>)}</tbody></table>}</div></div>}
